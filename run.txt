# You have to start the bot TWICE (once for config setup, after that only once to run it)
# Don't forget to edit config file!
# Once bot is running in discord sync commands using: <prefix or mention> sync_ctx

docker compose build
docker compose  up

# OR

python3 main.py
