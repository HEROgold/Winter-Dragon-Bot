[project]
name = "wd-bot"
version = "0.1.0"
description = "Full bot implementation for Winter Dragon"
readme = "README.md"
authors = [
    { name = "HEROgold", email = "martijnwieringa28@gmail.com" }
]
requires-python = ">=3.15"
dependencies = [
    "herogold[orm]>=3.0.0",
    "wd-config",
]

[tool.uv.sources]
wd-config = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
