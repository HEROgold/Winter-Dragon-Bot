[project]
name = "winter-dragon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "backend",
    "bot",
    "database",
]

[tool.setuptools]
packages = ["bot", "backend", "database", "tests"]

[tool.uv.workspace]
members = ["bot", "backend", "database", "tests"]

[tool.uv.sources]
bot = { workspace = true }
database = { workspace = true }
backend = { workspace = true }
