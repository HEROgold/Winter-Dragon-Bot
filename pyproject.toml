[project]
name = "Winter-Dragon-Bot"
requires-python = ">=3.12"
# authors = [{name = "name", email = "name@address.com"}]
version = "0.1.0"
dependencies = [
    "discord-py>=2.4.0",
    "uvicorn>=0.34.0",
]

[tool.uv.workspace]
members = [
    "bot",
    "website/backend/src", "tests", "database"]

[tool.setuptools]
py-modules = [
    "bot",
    "website.backend"
]
